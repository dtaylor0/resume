{
    "name": "app",
    "module": "index.js",
    "devDependencies": {
        "@types/bun": "latest",
        "concurrently": "^9.0.1",
        "nodemon": "^3.1.7"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "scripts": {
        "server": "cd server && bun run --watch server.js",
        "client": "cd client && bun run dev",
        "dev": "bun run concurrently \"bun run server\" \"bun run client\"",
        "build:client": "cd client && bun run build",
        "build:server": "cd server && bun build server.js --outdir ../dist/server --target node",
        "copy:server": "cp -R server/data dist/server && cp server/.env dist/server",
        "build": "bun run build:client && bun run build:server && bun run copy:server",
        "start": "cd dist/server && node server.js"
    },
    "trustedDependencies": [
        "faiss-node"
    ],
    "type": "module",
    "dependencies": {
        "faiss-node": "^0.5.1"
    }
}
